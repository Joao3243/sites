function direcione(event){
    if(event.which===13){
        let nome=document.querySelector('input').value
        let email=document.querySelectorAll('input')[1].value
        let password=document.querySelectorAll('input')[2].value
        
    }
        
}